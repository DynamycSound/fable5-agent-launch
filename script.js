const glow=document.querySelector('.glow');if(glow)window.addEventListener('pointermove',e=>glow.style.transform=`translate(${e.clientX}px,${e.clientY}px) translate(-50%,-50%)`);document.querySelectorAll('.code').forEach(box=>{const button=box.querySelector('button');if(button)button.addEventListener('click',async()=>{await navigator.clipboard.writeText(box.querySelector('pre').innerText);button.textContent='copied';setTimeout(()=>button.textContent='copy',1200)})});
